# Copy to .env, generate unique values, and keep the resulting file private.
POSTGRES_PASSWORD=replace-with-a-long-random-postgres-password
# Only the key required by the provider selected in infra/litellm/config.yaml is
# needed. Provider keys stay inside the LiteLLM container and never reach a harness.
OPENAI_API_KEY=replace-with-your-provider-key
# ANTHROPIC_API_KEY=replace-with-your-provider-key
# GEMINI_API_KEY=replace-with-your-provider-key
# OPENROUTER_API_KEY=replace-with-your-provider-key
# AZURE_API_KEY=replace-with-your-provider-key
# AZURE_API_BASE=https://your-resource.openai.azure.com
# AZURE_API_VERSION=2025-04-01-preview
LITELLM_MASTER_KEY=sk-replace-with-a-long-random-litellm-master-key
LITELLM_MODEL=agent-model
# Course generation is live-only; the selected harness still uses the same model alias.
AGENT_HARNESS=gemini-cli
NEO4J_PASSWORD=replace-with-a-long-random-neo4j-password
MINIO_ROOT_USER=learning-coach-minio
MINIO_ROOT_PASSWORD=replace-with-a-long-random-minio-password
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=replace-with-a-long-random-grafana-password
# Local HTTP port. Use a domain-specific production Caddy configuration for TLS.
HTTP_PORT=8080

# Browser output is bounded and wrapped as untrusted content before it is
# returned to Gemini. Destination validation is enforced by the MCP gateway.
AGENT_BROWSER_CONTENT_BOUNDARIES=1
AGENT_BROWSER_MAX_OUTPUT=6000
# Optional trusted argv override for development only. Normal installs resolve
# the pinned project-local agent-browser executable automatically.
# AGENT_BROWSER_COMMAND=agent-browser
