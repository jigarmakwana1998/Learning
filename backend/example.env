# Copy to backend/.env and replace every placeholder before deployment.
# Harness selection is independent of the model alias resolved by LiteLLM.
AGENT_HARNESS=gemini-cli
LITELLM_BASE_URL=http://127.0.0.1:4000
LITELLM_API_KEY=sk-replace-with-a-long-random-litellm-master-key
LITELLM_MODEL=agent-model
APP_NAME=Learning Coach API
APP_VERSION=0.4.0
ENVIRONMENT=development

# Supabase Postgres Session Pooler connection (SQLAlchemy async driver).
DATABASE_URL=postgresql+asyncpg://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_<your-key>

# Application-level secret.
APP_ENCRYPTION_KEY=replace-with-a-generated-fernet-key

# Optional live-harness CLI executable overrides. Gateway/model flags are enforced.
# CODEX_COMMAND=codex exec
# GEMINI_CLI_COMMAND=gemini
# ANTIGRAVITY_CLI_COMMAND=agy

# Safe browser research defaults. The MCP gateway overrides these values for
# each child process and accepts AGENT_BROWSER_COMMAND only as a trusted local
# development override.
AGENT_BROWSER_CONTENT_BOUNDARIES=1
AGENT_BROWSER_MAX_OUTPUT=6000
# AGENT_BROWSER_COMMAND=agent-browser
