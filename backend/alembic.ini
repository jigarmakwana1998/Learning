[alembic]
script_location = alembic
prepend_sys_path = .
# DATABASE_URL from backend/.env is used by alembic/env.py. This fallback is
# only for offline generation; set DATABASE_URL for every real migration.
sqlalchemy.url = postgresql+psycopg://learning_coach:learning_coach@localhost:5432/learning_coach
