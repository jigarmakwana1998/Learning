services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: learning_coach
      POSTGRES_USER: learning_coach
      POSTGRES_PASSWORD: learning_coach
    ports: ["5432:5432"]
    volumes: ["learning_coach_postgres:/var/lib/postgresql/data"]
volumes:
  learning_coach_postgres:
