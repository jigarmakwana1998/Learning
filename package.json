{
  "name": "learning-coach-browser-runtime",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "browser:install": "agent-browser install",
    "browser:doctor": "agent-browser doctor --offline --quick",
    "gemini:version": "gemini --version"
  },
  "dependencies": {
    "@google/gemini-cli": "0.51.0",
    "agent-browser": "0.27.3"
  }
}
